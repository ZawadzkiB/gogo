# Tech stack

**Purpose:** what the project is built with and the exact commands to build, run,
and test it — used by the plan, implement, and test phases.

<!-- gogo:meta
Mode: proxy
Source: [ ]            # e.g. ../../package.json, ../../README.md
Confidence: low
Generated-by: /gogo:build (scaffold)
-->
> Languages, frameworks, build/run/test commands. Source of truth: links above.

## Stack
- Language(s): <…>
- Framework(s): <…>
- Package manager: <npm | pnpm | yarn | pip | poetry | go | cargo | …>

## Commands (run from repo root)
- build:     `<cmd>`
- test:      `<cmd>`     # unit / integration
- e2e:       `<cmd>`     # if present
- lint:      `<cmd>`
- typecheck: `<cmd>`
- dev / run: `<cmd>`

## Services / runtime
<db, ports, env vars, docker; "none" if a pure library>

## gogo overrides
