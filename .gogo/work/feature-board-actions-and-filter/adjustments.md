# Adjustments — feature `board-actions-and-filter`

Running log of user-requested changes / clarifications during planning.

## 2026-07-02 — initial framing (from the request itself)

The user first sketched view/manage **modes**, then self-corrected: "or maybe
instead of implementing modes view/manage lets just add 'v' button that open view
of this item plan|work|changelog depends of where this item is on board" — the
plan adopts the action-key design as D1's recommendation.
