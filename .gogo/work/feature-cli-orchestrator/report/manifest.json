{
  "slug": "cli-orchestrator",
  "updated": "2026-07-11",
  "note": "As-built diagram set for Slice 1 (report ⑤). flow = the Go loop; shared-core = two orchestrators over one core; sequence = warm resume vs fresh eyes; class = the shipped Go structure. before/ holds the plan-time baseline for before/after compare.",
  "diagrams": [
    { "kind": "flow", "file": "report/flow.mmd", "title": "The Go orchestrator loop: warm dev resume, fresh review/test, issues.json routing" },
    { "kind": "flow", "file": "report/shared-core.mmd", "title": "Two orchestrators over one shared core (skills + typed contracts + the one routing rule)" },
    { "kind": "sequence", "file": "report/sequence.mmd", "title": "Warm developer resume across fix rounds vs fresh-eyes review/test" },
    { "kind": "class", "file": "report/class.mmd", "title": "As-built (Slice 1): the orchestrator package + launch.RunPhase/PhaseOpts + contract.Route" }
  ]
}
