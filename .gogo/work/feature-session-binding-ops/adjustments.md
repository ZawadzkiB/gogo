# Adjustments — feature `session-binding-ops`

Changes and clarifications the user asked for after a plan was presented. Each
entry: the request (verbatim where possible), what changed in `plan.md`, and the
date.

## 2026-07-31 — first review round (plan stays awaiting acceptance)

- **"Keep as plan for now"** — the user reviewed the plan and resolved every
  decision, but acceptance is deliberately deferred (implementation queued behind
  the in-flight `feature-notify-only-at-user-gates`). `state.md` stays
  `awaiting-plan-acceptance`; accept later via `/gogo:accept session-binding-ops`
  or the board, then `/gogo:go`.
- **All seven decisions resolved as recommended** (no plan-text changes needed):
  D1 card-only `P` · D2 manual re-bind + unbound detector · D3 move = rename only ·
  D4 action derived from target status · D5 card keys, no sessions screen ·
  D6 truthful name on shipped items + `K`/`R` · D7 version 0.32.0. RESOLVED blocks
  recorded in `decisions.md`.
