# UAT — feature `cockpit-cards-and-cli-awareness`

<!-- The UAT gate log — the plan-gate symmetry at the END of the pipeline.
  Phase ⑤ (report) ends at status: awaiting-uat; running `/gogo:done` IS the acceptance
  (no extra confirmation question, mirroring how accepting a plan unlocks `/gogo:go`).
  Issues/questions instead re-plan the SAME item (analyst round). Append-only, newest round at bottom. -->

## UAT round 1 — accepted (user, 2026-07-12) — via /gogo:done
Accepted the as-built work: (A) the on-demand `gogo-cli` companion reference + lean pointer + four-source enumeration-sync lint, and (B) the rich board drill-in card (description / folder / status / sessions / events tail with `a` attach and `K` kill). Review APPROVE, test green; TEST-002 hands-on proof user-skipped per D6.
